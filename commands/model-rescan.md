---
description: Re-validate with different LLM, show delta. Aliased to /labs --model-rescan.
---

# /model-rescan (alias)

This command is an **alias** for `/labs --model-rescan`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/labs --model-rescan` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/labs --model-rescan $@`

---
description: LLM-specific risk surface (prompt injection / MCP / AI-BOM). Aliased to /labs --llm.
---

# /llm (alias)

This command is an **alias** for `/labs --llm`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/labs --llm` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/labs --llm $@`

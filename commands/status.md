---
description: One-screen project + plugin health snapshot. Aliased to /posture --status.
---

# /status (alias)

This command is an **alias** for `/posture --status`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/posture --status` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/posture --status $@`

---
description: Estimate engineering hours per finding. Aliased to /labs --time-to-fix.
---

# /time-to-fix (alias)

This command is an **alias** for `/labs --time-to-fix`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/labs --time-to-fix` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/labs --time-to-fix $@`

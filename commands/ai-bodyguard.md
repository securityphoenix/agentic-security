---
description: Configure the AI bodyguard PreToolUse hook. Aliased to /setup --bodyguard.
---

# /ai-bodyguard (alias)

This command is an **alias** for `/setup --bodyguard`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/setup --bodyguard` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/setup --bodyguard $@`

---
description: Configure the destructive-Bash-command guard. Aliased to /setup --destructive-guard.
---

# /destructive-guard (alias)

This command is an **alias** for `/setup --destructive-guard`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/setup --destructive-guard` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/setup --destructive-guard $@`

---
description: Compliance attestation. Frameworks NIST, ASVS, LLM Top 10, EU AI Act. Aliased to /compliance --report.
---

# /compliance-report (alias)

This command is an **alias** for `/compliance --report`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/compliance --report` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/compliance --report $@`

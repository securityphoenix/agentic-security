---
description: Threat modeling views (STRIDE, personas, SPOF, etc). Aliased to /posture --threat.
---

# /threat (alias)

This command is an **alias** for `/posture --threat`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/posture --threat` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/posture --threat $@`

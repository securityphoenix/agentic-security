---
description: Walk-through tour of the plugin. Aliased to /secure --tour.
---

# /tutorial (alias)

This command is an **alias** for `/secure --tour`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/secure --tour` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/secure --tour $@`

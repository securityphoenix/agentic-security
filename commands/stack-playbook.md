---
description: Stack-specific posture playbook. Aliased to /posture --playbook.
---

# /stack-playbook (alias)

This command is an **alias** for `/posture --playbook`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/posture --playbook` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/posture --playbook $@`

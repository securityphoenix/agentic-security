---
description: Scanner self-test / version diff / concurrency check. Aliased to /scan --scanner-meta.
---

# /scanner (alias)

This command is an **alias** for `/scan --scanner-meta`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/scan --scanner-meta` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/scan --scanner-meta $@`

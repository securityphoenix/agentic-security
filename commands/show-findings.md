---
description: View findings table / HTML report. Aliased to /triage --show.
---

# /show-findings (alias)

This command is an **alias** for `/triage --show`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/triage --show` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/triage --show $@`

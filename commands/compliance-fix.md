---
description: Fix every Not-Compliant control in the compliance report. Aliased to /fix --compliance.
---

# /compliance-fix (alias)

This command is an **alias** for `/fix --compliance`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/fix --compliance` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/fix --compliance $@`

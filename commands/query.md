---
description: Natural-language security rule synthesizer. Aliased to /triage --query.
---

# /query (alias)

This command is an **alias** for `/triage --query`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/triage --query` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/triage --query $@`

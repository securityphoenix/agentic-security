---
description: Conversational SBOM exploration. Aliased to /supply --sbom.
---

# /sbom-explore (alias)

This command is an **alias** for `/supply --sbom`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/supply --sbom` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/supply --sbom $@`

---
description: Plain-English finding explanation. Aliased to /triage --explain.
---

# /explain (alias)

This command is an **alias** for `/triage --explain`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/triage --explain` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/triage --explain $@`

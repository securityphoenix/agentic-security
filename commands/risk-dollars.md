---
description: Expected-value-of-exploitation in USD per finding. Aliased to /labs --risk-dollars.
---

# /risk-dollars (alias)

This command is an **alias** for `/labs --risk-dollars`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/labs --risk-dollars` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/labs --risk-dollars $@`

---
description: Auditor walkthrough for a framework. Aliased to /compliance --walkthrough.
---

# /auditor-walkthrough (alias)

This command is an **alias** for `/compliance --walkthrough`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/compliance --walkthrough` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/compliance --walkthrough $@`

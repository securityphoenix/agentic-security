---
description: Roleplay attacker against a finding. Aliased to /triage --red-team.
---

# /red-team (alias)

This command is an **alias** for `/triage --red-team`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/triage --red-team` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/triage --red-team $@`

---
description: Look up sibling-repo fixes + triage decisions. Aliased to /labs --cross-repo.
---

# /cross-repo-recall (alias)

This command is an **alias** for `/labs --cross-repo`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/labs --cross-repo` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/labs --cross-repo $@`

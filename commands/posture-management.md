---
description: Posture mgmt surface (auth/network/WAF/telemetry imports). Aliased to /posture --mgmt.
---

# /posture-management (alias)

This command is an **alias** for `/posture --mgmt`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/posture --mgmt` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/posture --mgmt $@`

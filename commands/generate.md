---
description: Generate security artifacts (privacy / disaster / tests / social). Aliased to /fix --generate.
---

# /generate (alias)

This command is an **alias** for `/fix --generate`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/fix --generate` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/fix --generate $@`

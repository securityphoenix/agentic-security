---
description: Score AI agent harness against six-domain rubric. Aliased to /posture --harness.
---

# /harness-score (alias)

This command is an **alias** for `/posture --harness`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/posture --harness` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/posture --harness $@`

---
description: List the 9 primary commands + legacy aliases. Aliased to /secure --help.
---

# /help (alias)

This command is an **alias** for `/secure --help`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/secure --help` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/secure --help $@`

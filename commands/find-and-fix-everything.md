---
description: Batch fix every severity. Aliased to /fix --all.
---

# /find-and-fix-everything (alias)

This command is an **alias** for `/fix --all`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/fix --all` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/fix --all $@`

---
description: Findings trend over time. Aliased to /posture --trend.
---

# /security-trend (alias)

This command is an **alias** for `/posture --trend`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/posture --trend` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/posture --trend $@`

---
description: Install pre-commit security hook (husky/pre-commit/lefthook/native). Aliased to /setup --hooks.
---

# /install-hooks (alias)

This command is an **alias** for `/setup --hooks`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/setup --hooks` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/setup --hooks $@`

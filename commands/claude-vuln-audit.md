---
description: Analyze patterns in Claude-introduced findings. Aliased to /labs --claude-audit.
---

# /claude-vuln-audit (alias)

This command is an **alias** for `/labs --claude-audit`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/labs --claude-audit` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/labs --claude-audit $@`

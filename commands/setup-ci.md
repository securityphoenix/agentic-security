---
description: Generate CI workflow YAML for your provider. Aliased to /setup --ci.
---

# /setup-ci (alias)

This command is an **alias** for `/setup --ci`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/setup --ci` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/setup --ci $@`

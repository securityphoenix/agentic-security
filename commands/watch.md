---
description: Continuous incremental scan. Aliased to /scan --watch.
---

# /watch (alias)

This command is an **alias** for `/scan --watch`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/scan --watch` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/scan --watch $@`

---
description: PR-description security delta + ATT&CK + reviewers. Aliased to /compliance --pr.
---

# /pr-augment (alias)

This command is an **alias** for `/compliance --pr`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/compliance --pr` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/compliance --pr $@`

---
description: Trim unused code or unused packages. Aliased to /fix --trim.
---

# /trim (alias)

This command is an **alias** for `/fix --trim`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/fix --trim` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/fix --trim $@`

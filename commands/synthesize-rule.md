---
description: Draft custom SAST detector from natural-language description. Aliased to /labs --synthesize-rule.
---

# /synthesize-rule (alias)

This command is an **alias** for `/labs --synthesize-rule`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/labs --synthesize-rule` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/labs --synthesize-rule $@`

---
description: A-F letter grade + one explanation + one next action. Aliased to /posture --report-card.
---

# /report-card (alias)

This command is an **alias** for `/posture --report-card`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/posture --report-card` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/posture --report-card $@`

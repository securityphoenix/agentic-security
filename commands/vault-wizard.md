---
description: Guided env-var → secrets vault migration. Aliased to /fix --vault.
---

# /vault-wizard (alias)

This command is an **alias** for `/fix --vault`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/fix --vault` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/fix --vault $@`

---
description: Historical analysis — line-level authorship + originating prompt. Aliased to /scan --archaeology.
---

# /archaeology (alias)

This command is an **alias** for `/scan --archaeology`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/scan --archaeology` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/scan --archaeology $@`

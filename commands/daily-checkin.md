---
description: Daily security digest. Aliased to /secure --daily.
---

# /daily-checkin (alias)

This command is an **alias** for `/secure --daily`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/secure --daily` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/secure --daily $@`

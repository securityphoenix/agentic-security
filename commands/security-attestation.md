---
description: Buyer-facing security posture artifact. Aliased to /compliance --attestation.
---

# /security-attestation (alias)

This command is an **alias** for `/compliance --attestation`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/compliance --attestation` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/compliance --attestation $@`

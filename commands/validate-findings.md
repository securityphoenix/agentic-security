---
description: Verify a finding is exploitable (PoC + variants). Aliased to /triage --validate.
---

# /validate-findings (alias)

This command is an **alias** for `/triage --validate`. The capability is preserved exactly; this file is kept for back-compat with users who have muscle memory.

When invoked, execute `/triage --validate` with any additional arguments passed through.

For the full command surface, see `/secure --help` (or the legacy `/help`).

Forwards to: `/triage --validate $@`
